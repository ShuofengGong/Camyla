# Draft Mode

## Task Overview

You are starting a new implementation from scratch. Create a complete, working solution based on the task requirements.

---

## 📝 Creating New Code

Your task is to create `experiment.py` and `test.py` from scratch.

**Workflow**:

1. Analyze the task requirements carefully
2. Create `experiment.py` with the complete implementation
3. Create `test.py` with unit tests
4. Run tests to verify the code works
5. Fix any issues and repeat until tests pass

---

## Code Generation Instructions

**Target Files**: Create or update `experiment.py` and `test.py` in the workspace

> ⚠️ **IMPORTANT**: Only `experiment.py` will be delivered. Do NOT create alternative files (e.g., `experiment_new.py`). Fix bugs in the original file.

### Implementation Checklist

1. ✅ **MUST USE `if __name__ == "__main__":` guard** (CRITICAL!)
2. ✅ Wrap all executable code in a `main()` function
3. ✅ Put only imports and configuration variables at module level
4. ✅ Must follow the CamylaNet framework documentation
5. ✅ Use the exact dataset configuration specified
6. ✅ Handle errors gracefully with try-except blocks
7. ✅ Print clear output showing experiment progress and results

---

## Variables

| Variable | Description |
|----------|-------------|
| (none) | Draft mode has no dynamic variables |
